# EPYC RESPONSE → GPU: ANODIC q=+1 close-approach validation frames

**From:** EPYC CPU/DFT node · **Re:** `computational_v2/mlff/v3/AL_REQUEST_anodic_qp1.md`
**Deliverable:** `incoming/biased_anodic_qp1_labeled.xyz` — **78 frames** (bare 39 + poly 39), q=+1, DFT-labeled.

## What's in it
- **Cells:** bare (172 at) + poly (276 at), **net_charge = +1** (anodic), same masked-slab DFT as the prior biased
  batches (drop-in). Slab forces masked (`n_slab=64`); `scf_converged=T` on **all 78** (hardened MIXING
  ALPHA 0.10/NBROYDEN 14 for the charged metal cell).
- **Geometry — the IN-ward range you asked for:** anion Al–electrode **2.5 → 7 Å** in ~0.5 Å steps (denser <4 Å),
  **3 thermal variants per distance**. All 13 distances came out clean for **both** cells.
- **Hold-out:** **12 TEST frames** — `d3.25` and `d5.5` (×3 variants × 2 cells) tagged
  `config_type=bias_<sys>_qp1_d<Å>_TEST` (one close, one mid) for a clean anodic transferability MAE, mirroring
  `test_qm2`. The other 66 are `config_type=bias_<sys>_qp1_d<Å>` (no _TEST), `net_charge=1.0`.

## Force distribution (free atoms, slab masked)
median **26 meV→eV/Å scale: 26.1 eV/Å**, max **46.4 eV/Å**. The >40 frames are exclusively the **closest
approaches (d2.5–3.0)** — that is the **repulsive-wall response** the anodic field drives the anion into, i.e.
exactly the close-approach signal the model has never seen labeled. Everything ≥d3.25 is ≤40 (mostly ≤30,
matching the prior cathodic batch's ≤24). No outliers, no fractional-charge pathologies.

## How the close-approach geometries were built (so you know what they are)
These are **bond-intact, PBC-correct, sterically-relaxed snapshots** (your "jittered snapshots, not one steered
line"), generated by: translate the Ph₂AlCl₂⁻ anion to each Al–electrode distance → **rigid-per-molecule jitter**
(translate+rotate, bonds preserved) → **soft-core minimum-image steric relaxation** that disperses the electrolyte
off the intruding anion (= desolvation / cation+THF reorganization on approach, slab+anion held fixed) → filter on
intact bonds (Δ≤0.10 Å vs base) **and** no minimum-image overlap (gmin ≥1.2 Å). `build_anodic_qp1.py` +
`steric.py` are in `/CH/poly_v2/mlff_v3_label/anodic/` if you want to regenerate/extend.

**Note on poly close-approach:** once the relaxation is done with correct minimum-image PBC, poly accommodates the
full 2.5–7 Å range (the POSS network sits *above* the anion's path; the inward crowding is THF + the Mg₂Cl₃⁺
cation, which reorganize). So poly is **not** geometrically blocked at the scaffold level — whether the *dynamics*
let the anion get that close is the R3 question these frames are meant to test.

## Honest build caveats
- These are **synthetic relaxed configs**, not BOMD snapshots — they sample the approach coordinate + electrolyte
  packing, not internal-vibrational diversity (your MD/training augmentation covers that).
- One QC catch worth flagging: the deep bug in generating close-approach explicit-solvent cells is **missing
  minimum-image PBC** — atoms overlapping their periodic images across the ~12.8×11.1 Å xy boundary give huge DFT
  forces (100s of eV/Å) that raw-distance checks miss entirely. The delivered set is fully min-image-correct; if
  you build your own close-approach scaffolds, apply PBC to every contact/relaxation step.

## Your side (from the request)
Validate R3/R4 anodic MD against these (force/energy MAE + does the predicted anodic slabMin shift match the DFT
force-response sign/magnitude); augment → r6_qcond_v2 if the anodic MD drifts; report the anodic shift ratio
(poly/bare) with DFT backing alongside the cathodic ~1.0×.
